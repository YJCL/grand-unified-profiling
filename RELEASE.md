# アプリリリース 条件定義・チェックリスト

最終更新: 2026-07-03（価格・決済記述を現行方針に統一）

> ⚠️ **2026-06-19注記（継続）**: 本書はStripe前提で書かれているが**Stripeアカウントは停止済み（占い業種NG）**。決済の実装(Stripe)チェックリストは無効。env(`AUTH_SECRET`/`RESEND`/`ADMIN_KEY`/`VAPID`)は設定済み・`orba.life`稼働中・特商法/プライバシーは実情報入り。
>
> ⚠️ **2026-07-03追記**: プレミアム想定価格を **¥500〜550/月 → ¥3,000/月** に更新（少数高単価モデルへの方針転換。詳細は`ROADMAP.md`）。決済レールはococala/ココナラ定期購入などが候補で**未確定**。また、Orba本体とは別に**ココナラでの単発PDF販売（¥10,000・商品A）が既に稼働中**（外部チャネル。詳細は`orba_sales_strategy_handoff.md`）。本チェックリストはOrba本体アプリ自体のリリース条件であり、ココナラ側の状態とは独立して読むこと。

## 1. 配信形態の選択（最重要の意思決定）

| 形態 | 出し方 | 決済 | 備考 |
|------|--------|------|------|
| **PWA（推奨・第一段階）** | 今のWebをそのまま「ホーム画面に追加」でアプリ化 | Web決済（占い業種はStripe等の自己登録型が軒並みNG。レール未確定） | 審査なし・即日。installable済み |
| App Store (iOS) | Capacitor等でラップして提出 | Apple In-App Purchase必須（手数料15〜30%）。デジタルコンテンツへの外部決済誘導は審査リジェクト対象 | Apple Developer Program 年99ドル、審査あり（占いカテゴリは通常可） |
| Google Play | 同上 | Google Play Billing（15〜30%） | 登録25ドル（買い切り）、審査は比較的緩い |

**現在の推奨ロードマップ**: PWAのまま、決済レール確定待ち（P2、`ROADMAP.md`参照）→ 収益シグナルが出たらストア版(IAP対応)を検討。

## 2. リリース前に必須（ブロッカー）

- [ ] ~~決済の実装: Stripe~~ **無効（Stripeアカウント停止済み）**。代替レール（ococala／ココナラ定期購入等）を`ROADMAP.md` P2で確定させ、確定後に本項目を更新すること。
- [ ] **Vercel環境変数 `AUTH_SECRET`**: 未設定だと開発用の弱い鍵で動く。設定必須（設定後に再デプロイ）
- [x] **特定商取引法に基づく表記**: `/legal/tokushoho` 作成済み。→ **ページ冒頭の `SELLER` を実情報（氏名/屋号・連絡先メール）に差し替えること**（住所/電話は「請求に応じ開示」で可）
- [x] **パスワードリセット**: 実装済み（`/forgot`→メール→`/reset`）。→ **メール送信に Resend のセットアップが必要**：
  1. resend.com でアカウント作成 → API Key 発行
  2. 送信ドメイン `orba.life` を Resend で認証（指定のDNSレコードをお名前.comに追加）
  3. Vercel環境変数に `RESEND_API_KEY` と `EMAIL_FROM=Orba <noreply@orba.life>` を設定 → Redeploy
  ※ 未設定の間はリセットメールが送信されません（フロー自体は動作）
- [ ] **お問い合わせ窓口**: プライバシーポリシー内の連絡先を実際のメールアドレス等に差し替え

## 3. リリース前に強く推奨

- [ ] レート制限（連打・bot対策。Vercel WAF か upstash/ratelimit）
- [ ] メールアドレス確認（誤入力・なりすまし対策）
- [ ] カスタムドメイン **`orba.life`（取得済み）** を Vercel → Settings → Domains に追加し、`NEXT_PUBLIC_APP_URL=https://orba.life` を設定して Redeploy（SSLは自動発行）
- [ ] DBバックアップ方針の確認（SupabaseはFreeプランで7日PITRなし→課金検討）
- [ ] エラー監視（Vercel Logs / Sentry）
- [ ] 多言語（en/es）は現在UI辞書のみ・会話はja固定 → 当面jaのみで出すのが現実的

## 4. 実装済み（今回まで）

- ✅ 決定論的占術エンジン（西洋占星術／四柱推命／九星気学／宿曜／数秘術／ヒューマンデザイン／干支分析／天体トランジット）＋LLM解釈レイヤー
- ✅ 易占いMVP（三枚コイン法・本卦/変爻/之卦・履歴保存・24時間再利用制限）
- ✅ アカウント（メール＋パスワード、scryptハッシュ、httpOnlyセッション）／インスタント→本登録の二軸
- ✅ **サーバー側の有料ゲート**:
  - isPremium はクライアントから変更不可（`/api/billing/upgrade` のみ、要ログイン＋本登録）
  - 無料: チャット1日3回（DBカウンタで強制）／運気カレンダー7日分
  - プレミアム: チャット無制限／カレンダー最大60日
  - 本登録済みユーザーのデータはセッション必須（他人のuserIdを知っていてもアクセス不可）
- ✅ PWA: manifest / アイコン(192/512/maskable/apple-touch) / theme-color / standalone表示
- ✅ 法務ページ: 利用規約・プライバシーポリシー（連絡先は要差し替え）
- ✅ 会話型オンボーディング、チャット履歴表示、引き継ぎコード
- ✅ 結果シェア(`/s/[id]`+OGP)、チケット制度、Webプッシュ通知、`/admin/stats`ファネル計測

## 5. 課金プラン（想定・レール未確定）

| プラン | 制限 | 想定価格 |
|--------|------|--------|
| 無料 | チャット3回/日・カレンダー7日・複数プロフィール不可（UI上） | ¥0 |
| プレミアム | チャット無制限・カレンダー60日・複数プロフィール | **¥3,000/月想定**（旧¥500〜550から2026-07-03に更新。少数高単価モデルへの転換） |

※ 価格・プラン構成は決済レール確定時に最終決定。`isPremium: Boolean` を `plan: String` に変更する拡張も引き続き検討候補。

## 6. ストア版を出す場合の追加要件（第二段階）

- Capacitorラップ、スプラッシュ画面、プッシュ通知（FCM/APNs）
- Apple: IAP実装・App用プライバシーラベル・審査用デモアカウント
- スクリーンショット（6.7"/6.1"/iPad等）・ストア説明文
- 「占い」アプリはエンタメカテゴリ。医療・金融助言を行わない旨の明記（実装済みの免責でOK）
