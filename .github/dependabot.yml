# 依存関係の脆弱性を継続的に監視し、修正PRを自動作成する（脆弱性診断の自動化）
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      all-minor-patch:
        update-types: ["minor", "patch"]
